{{!< default}}
<header id="blog-header"{{#if @site.cover_image}} class="has-cover"{{/if}}>
	<div class="inner">
		<h1 class="blog-name"><a href="{{@site.url}}">{{@site.title}}</a></h1>
		{{#if @site.description}}
		<span class="blog-description">{{@site.description}}</span>
		{{/if}}
		{{#if @site.cover_image}}
		<div class="blog-cover cover" style="background-image: url('{{img_url @site.cover_image size="xl"}}');"></div>
		{{/if}}
	</div>
</header>

<div id="index" class="container">

	<main class="content" role="main">

		{{> "loop"}}

	</main>

</div>
