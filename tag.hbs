{{!< default}}


<header id="blog-header"{{#if tag.feature_image}} class="has-cover"{{else}}{{#if @site.cover_image}} class="has-cover"{{/if}}{{/if}}>
	<div class="inner">
		<h1 class="blog-name">{{tag.name}}</h1>
		<span class="blog-description">
		{{#if tag.description}}
			{{tag.description}}
		{{else}}
			{{plural pagination.total empty=(t "No Posts") singular=(t "% Post") plural=(t "% Posts")}}
		{{/if}}
		</span>
		{{#if tag.feature_image}}
		<div class="blog-cover cover" style="background-image: url('{{img_url tag.feature_image size="xl"}}');"></div>
		{{else}}
		{{#if @site.cover_image}}
		<div class="blog-cover cover" style="background-image: url('{{img_url @site.cover_image size="xl"}}');"></div>
		{{/if}}
		{{/if}}
	</div>
</header>

<div id="index" class="container">

	<main class="content" role="main">

		{{> "loop"}}

	</main>

</div>
